# Local capture fixtures

These pages are deliberately local test inputs for later capture milestones. They must never be deployed as a service or used with real credentials. `authenticated.html` only checks the harmless `postkeeper-fixture-auth=1` cookie set by its companion page.
